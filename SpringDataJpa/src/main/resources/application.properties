spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/postgres?currentSchema=postgres
spring.datasource.username=postgres
spring.datasource.password=postgresmaster

#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=sa
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect


#To see the sql worked on application, use the parameter below.
spring.jpa.show-sql=true


#To format the sql commands, use the parameter below.
spring.jpa.properties.hibernate.format_sql=true

#This parameter run inserts in /resources/data.sql
spring.jpa.defer-datasource-initialization=true
#if data.sql can not be found, open the parameter below.
spring.sql.init.data-locations=classpath:data.sql

#spring.datasource.initialize=false

#There both parameters are for same aim.
#The first one is more clear and chose for spring projects more
#spring.jpa.properties.hibernate.hbm2ddl.auto=update
#spring.jpa.hibernate.ddl-auto=update
